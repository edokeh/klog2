<nav>
  <ul>
    <li>
      <%= link_to "Blog", '/', :class => nav_class('blog') %>
    </li>
    <li>
      <%= link_to "Archives", archive_path, :class => nav_class('archive') %>
    </li>
    <% @pages.each do |page| %>
        <li>
          <%= link_to page.title, page_path(page.slug), :class => nav_class("page_#{page.slug}") %>
        </li>
    <% end %>
  </ul>
</nav>